# ── Core Web Framework ──────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.2
python-multipart==0.0.26

# ── Security & Auth ─────────────────────────────────────────
bcrypt==4.2.1

# ── System Control ──────────────────────────────────────────
pyautogui==0.9.54
pyperclip==1.9.0
psutil==6.1.1

# ── Screen Capture & Streaming ──────────────────────────────
opencv-python-headless==4.10.0.84
numpy==1.26.4
mss==9.0.2

# ── Windows-only: DXCam for faster screen capture ───────────
# Automatically skipped on Linux/Mac by pip
dxcam==0.3.2; sys_platform == "win32"

# ── Linux-only: Virtual keyboard ────────────────────────────
# Automatically skipped on Windows/Mac by pip
# Requires /dev/uinput access (sudo or udev rule)
uinput; sys_platform == "linux"

# ── WebRTC ──────────────────────────────────────────────────
aiortc==1.9.0
av==14.2.0
aioice==0.9.0

# ── System Tray (optional — for --tray flag) ────────────────
pystray==0.19.5
Pillow==12.2.0

# ── Optional (Performance & Features) ───────────────────────

# PyTurboJPEG      - Hardware-accelerated JPEG encoding (lower CPU)
# pip install PyTurboJPEG

# cryptography     - HTTPS/SSL certificate generation
# pip install cryptography

# ── External Tools (must install separately) ────────────────
# FFmpeg  - H264 hardware encoding (h264_amf/h264_nvenc/h264_qsv)
#   Windows:  winget install ffmpeg
#   Linux:    sudo apt install ffmpeg
#   macOS:    brew install ffmpeg
#   *Without FFmpeg, streaming falls back to JPEG mode.

# Linux extras:
#   sudo apt install xdotool xclip xsel libportaudio2

# ── Platform Notes ──────────────────────────────────────────
# Python:   3.10+ (3.11/3.12 recommended)
#           *Avoid 3.14 on Windows due to asyncio ProactorEventLoop issues.
# Windows:  7+ (10/11 recommended)
# Linux:    Ubuntu 20.04+, Debian 11+, or any modern distro
# macOS:    10.14 (Mojave) or later
